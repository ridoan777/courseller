SOFTVENCE
28 July 2025
------------------------

[1] PROJECT::

   -- Course creation web page
   -- The page should enable users to create courses with multiple modules, each containing multiple content items. 
   - Demo: https://www.loom.com/share/8103fc8162dc45f6b92e88de67d96046?sid=7fe0d9ba-4f75-4927-868b-62e297441035

[2] TECH::

   -- Frontend:
      - html
      - css
      - vanilla js
      - jquery

   -- backend:
      - php
      - laravel

[3] STRUCTURE::

   -- Container structure:

      - homepage:
         i. many courses

         - create courses:
            i. course title
            ii. feature video

            - multiple module (accordion):
               i. module title

               - multiple content items:
                  i. content title
                  ii. video source type
                  iii. video url
                  iv. video length

   -- Behind the scene:

      - validation
      - data storage
      - input form validation, like required in HTML
      - error handling with feedback



   fucntion outer() {
      let count = 0;

      return function inner(){
         count++;
            console.log(count);
      }
   }

   const inc = outer(); // closour
   inc();   // 1
   inc();   // 2
   inc();   // 3